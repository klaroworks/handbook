# Klaro Hub는 어떻게 작동하나

Hub가 "쓸 만한 AI"를 만드는 방식입니다. 화려한 데모보다 틀리지 않는 것에서 시작합니다. 조직의 문서·DB·API를 연결해 AI가 근거를 포함한 결과를 만들고, 담당자의 검토·승인과 감사 이력까지 한 환경에서 관리하는 Governed AI Engine입니다.

<div align="center">
  <picture>
    <source media="(prefers-color-scheme: dark)" srcset="https://raw.githubusercontent.com/klaroworks/handbook/main/assets/hub-dark.png">
    <img src="https://raw.githubusercontent.com/klaroworks/handbook/main/assets/hub-light.png" alt="연결 → 근거 → 초안 → 검토·승인 → 감사" width="100%">
  </picture>
</div>

## 근거를 먼저 모은다

답변은 조직의 문서·DB·API에서 원문을 인용·조회해 구성합니다. 모델의 기억이 아니라 검색된 근거가 1차 소스입니다. 검색은 키워드와 벡터를 함께 쓰고(하이브리드), 필요하면 질의를 다시 써서 다시 찾습니다. 찾은 근거는 순위를 다시 매겨 수치를 확인합니다.

## 지어내지 않는다

검색 결과의 신뢰도를 등급으로 판정하고, 부족하면 재작성·재검색으로 교정합니다. 그래도 근거가 모자라면 안내로 넘어갑니다. 없는 답을 만들지 않습니다. 이 교정 루프를 교정형 RAG(CRAG)라고 부릅니다.

## 공공 문서 그대로 낸다

한글(HWPX) 네이티브 출력을 지원합니다. 공공 조달에서 사실상 필수인데 글로벌 제품은 잘 다루지 않는 영역이라, 우리가 직접 만듭니다. 근거 인용 답변과 장문 보고서도 같은 방식으로 냅니다.

## 사람이 통제한다

검토·승인 큐를 MCP Elicitation 표준 위에 올렸습니다. 담당자가 근거와 수치를 확인한 뒤 승인해야 결과가 됩니다. 오답은 데이터 보완 루프로 되돌려 고치고, 운영 통계 대시보드로 상태를 봅니다.

## 느낌이 아니라 숫자로 말한다

"좋아졌다"는 인상이 아니라 지표로 판단합니다. 사실·수치 정확도, 근거 충족률, 최신성, 담당자 수정률, FP/FN, 분석 리드타임을 놓고 개선합니다.

## 폐쇄망에서 단독으로 돈다

vLLM·Ollama에 LoRA를 얹어 자체 서빙합니다. 외부 API 없이도 기관 환경 안에서 단독 운영되고, 데이터가 밖으로 나가지 않습니다. 외부 서비스가 붙여 쓰도록 SDK 두 종(guest-sdk·api-sdk)과 GUEST 유저 모델도 엽니다. 게스트 스트림에는 HITL 이벤트가 그대로 흐르므로, 외부 서비스도 검토·승인 흐름을 그대로 받습니다.

## 제품 사양

| 항목 | 내용 |
|---|---|
| **자동화 대상** | 규정·회계 질의응답, 문서 검색·분석, 보고서 생성, 기관 지식 상담 |
| **핵심 엔진** | 교정형 RAG(CRAG) · 하이브리드 벡터 검색 + 질의 재작성 · 멀티모달 확장(비전 임베딩 식별, 음성 STT/TTS) |
| **검증 방식** | 원문 근거 인용 강제 · 검색 신뢰도 등급 판정 · 근거 부족 시 안내형 전환 |
| **승인 체계** | HITL 검토·승인 워크플로 (MCP Elicitation 표준 구현) |
| **산출물** | 근거 인용 답변 · 장문 보고서 · 한글(HWPX) 네이티브 문서 |
| **배포 형태** | 온프레미스 설치형(폐쇄망·에어갭 가능) · 자체 모델 서빙 vLLM·Ollama + LoRA |
| **거버넌스** | RBAC 권한 관리 · 전 행위 감사 로그 · 크레딧 미터링 과금 |
| **연동** | 문서·DB·API 커넥터 · 엔진 외부 개방 SDK/API |
