# Klaro Hub는 어떻게 작동하나

Hub가 "쓸 만한 AI"를 만드는 방식입니다. 화려한 데모보다 틀리지 않는 것에서 시작합니다.

<div align="center">
  <img src="https://raw.githubusercontent.com/klaroworks/handbook/main/assets/hub-pipeline.png" alt="연결 → 근거 → 초안 → 검토·승인 → 감사" width="100%">
</div>

## 근거를 먼저 모은다

답변은 조직의 문서·DB·API에서 원문을 인용·조회해 구성합니다. 모델의 기억이 아니라 검색된 근거가 1차 소스입니다. 검색은 키워드와 벡터를 함께 쓰고(하이브리드), 찾은 결과를 다시 순위 매겨 수치를 확인합니다.

## 지어내지 않는다

검색 결과의 신뢰도를 등급으로 판정하고, 부족하면 질의를 다시 쓰거나 재검색으로 교정합니다. 그래도 근거가 모자라면 안내로 넘어갑니다. 없는 답을 만들지 않습니다. 이 교정 루프를 교정형 RAG(CRAG)라고 부릅니다.

## 공공 문서 그대로 낸다

한글(HWPX) 네이티브 출력을 지원합니다. 공공 조달에서 사실상 필수인데 글로벌 제품은 잘 다루지 않는 영역이라, 우리가 직접 만듭니다.

## 사람이 통제한다

검토·승인 큐를 MCP Elicitation 표준 위에 올렸습니다. 담당자가 근거와 수치를 확인한 뒤 승인해야 결과가 됩니다. 오답은 데이터 보완 루프로 되돌려 고칩니다.

## 느낌이 아니라 숫자로 말한다

"좋아졌다"는 인상이 아니라 지표로 판단합니다. 사실·수치 정확도, 근거 충족률, 최신성, 담당자 수정률, FP/FN, 분석 리드타임을 놓고 개선합니다.

## 폐쇄망에서 단독으로 돈다

vLLM·Ollama에 LoRA를 얹어 자체 서빙합니다. 외부 API 없이도 기관 환경 안에서 단독 운영되고, 데이터가 밖으로 나가지 않습니다. 권한 관리(RBAC), 전 행위 감사 로그, 사용량 미터링이 함께 들어 있습니다.

## 밖으로 연다

외부 서비스가 Hub를 붙여 쓸 수 있도록 SDK 두 종(guest-sdk·api-sdk)과 GUEST 유저 모델을 제공합니다. 게스트 스트림에는 HITL 이벤트가 그대로 흐르므로, 외부 서비스도 검토·승인 흐름을 그대로 받습니다.
