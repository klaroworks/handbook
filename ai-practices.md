# AI 개발 원칙

우리가 "쓸 만한 AI"를 만드는 방식입니다. 화려한 데모가 아니라 **틀리지 않는 것**에서 시작합니다.

## 근거 우선 (Retrieval-Grounded)
답변은 조직의 문서·DB·API에서 **원문을 인용·조회**해 구성합니다. 모델의 기억이 아니라 검색된 근거가 1차 소스입니다.

## 지어내지 않기 (Corrective RAG)
검색 결과의 신뢰도를 **등급으로 판정**하고, 부족하면 질의를 재작성하거나 재검색으로 교정합니다. 그래도 근거가 모자라면 **안내형으로 전환** — 없는 답을 만들지 않습니다. (교정형 RAG / CRAG 접근)

## 공공 문서 그대로 (HWPX Native)
공공 조달의 사실상 필수 요건인 **한글(HWPX) 네이티브 출력**을 지원합니다. 글로벌 제품이 잘 다루지 않는 영역이라, 우리가 직접 만듭니다.

## 사람이 통제 (HITL)
검토·승인 큐를 국제 표준(MCP Elicitation) 위에 올립니다. 오답은 데이터 보완 루프로 되돌려 개선합니다.

## 평가로 말한다
"좋아졌다"는 느낌이 아니라 **측정**으로 판단합니다. 검색 정확도·근거 충실도·승인 반려율을 지표로 두고 개선합니다.

## 폐쇄망에서 돈다
vLLM·Ollama 자체 서빙 + LoRA로 **외부 API 없이도 단독 운영**되도록 만듭니다. 데이터 외부 반출이 없습니다.
